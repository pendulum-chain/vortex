[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 10000

[profile.default.fuzz]
runs = 512

# Mainnet fork tests (task 2) read this endpoint from env:
# ETH_RPC_URL must be set; fork tests are skipped without it.
