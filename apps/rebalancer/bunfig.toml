[test]
# Coverage report config; the gate itself lives in scripts/check-coverage.ts.
# Without the ignore pattern, the built shared bundle (~158k lines) dominates the
# denominator and the ratchet measures shared's coverage instead of this app's.
coverageSkipTestFiles = true
coveragePathIgnorePatterns = ["**/packages/shared/dist/**"]
